syntax = "proto3";

import "RogueRecord.proto";

message RogueChallengeInfo {
    bool is_in_challenge = 1;
    repeated uint32 challenge_reward_list = 3;
    uint32 histroy_max_score = 4;
    repeated RogueRecord record_list = 5;
    uint32 record_slot = 6;
}
